defaults:
  - _self_
  - hydra: defaults
  - train: defaults
  - dataset: defaults
  - dpddm: base
  - model: defaults
  - wandb_cfg: defaults
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled
  
from_pretrained: False
seed: 57